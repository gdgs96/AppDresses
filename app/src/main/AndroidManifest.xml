<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appdresses">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.main.view.editOrder2Activity"></activity>
        <activity android:name=".presentation.main.view.editOrderActivity" />
        <activity android:name=".presentation.main.view.progressOrderActivity" />
        <activity android:name=".presentation.main.view.progressMedidaActivity" />
        <activity android:name=".presentation.main.view.order3Activity" />
        <activity android:name=".presentation.main.view.order2Activity" />
        <activity android:name=".presentation.main.view.orderActivity" />
        <activity android:name=".presentation.main.login.ForgotPassActivity" />
        <activity android:name=".presentation.main.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.main.login.RegisterActivity" />
        <activity android:name=".presentation.main.view.MainActivityNav" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>